export class Location {
  constructor({ city, county, longitude, latitude, state, zip_code }) {
    this.city = city;
    this.county = county;
    this.longitude = longitude;
    this.latitude = latitude;
    this.state = state;
    this.zip_code = zip_code;
  }
}
